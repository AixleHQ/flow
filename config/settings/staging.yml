agents:
  # See config/settings/production.yml — one registry prefix instead of seven
  # per-runtime entries.
  image_prefix: <%= ENV['AGENT_IMAGE_PREFIX'] || 'ghcr.io/aixle/aixle-app-' %>
  image_tag: <%= ENV['AGENT_IMAGE_TAG'] || '' %>
