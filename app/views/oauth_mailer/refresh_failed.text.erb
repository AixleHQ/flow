Your <%= @provider %> connection stopped working.

We tried to refresh its access automatically several times and it kept failing, so
we've marked it as needing attention. To restore access, reconnect the integration<%= @connect_url ? ":\n\n#{@connect_url}" : " from your integrations page." %>

— <%= Settings.app_name rescue "Aixle" %>
