# Repository language

Write all repository documentation and commit messages in English. This applies to design documents, implementation notes, review reports, runbooks and new documentation alike.

Before publishing a branch, check its documentation and commit messages. When correcting a language violation in explicitly authorized PR history, replace the affected commit rather than preserving a noncompliant document version in an additional commit.
