apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web
  namespace: palad-dev
spec:
  entryPoints:
    - web
  routes:
    - match: (Host(`palad.ai`) || Host(`www.palad.ai`)) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: web
          port: 4000
