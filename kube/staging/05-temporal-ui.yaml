apiVersion: apps/v1
kind: Deployment
metadata:
  name: temporal-ui
  namespace: palad-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temporal-ui
  template:
    metadata:
      labels:
        app: temporal-ui
    spec:
      containers:
        - name: temporal-ui
          image: temporalio/ui:2.43.3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          env:
            - name: TEMPORAL_ADDRESS
              value: temporal:7233
            - name: TEMPORAL_UI_PORT
              value: "8080"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: temporal-ui
  namespace: palad-staging
spec:
  selector:
    app: temporal-ui
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: temporal-ui
  namespace: palad-staging
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`temporal.staging.palad.ai`)
      kind: Rule
      middlewares:
        - name: staging-basic-auth
      services:
        - name: temporal-ui
          port: 8080
  tls:
    certResolver: letsencrypt
    domains:
      - main: staging.palad.ai
        sans:
          - temporal.staging.palad.ai
