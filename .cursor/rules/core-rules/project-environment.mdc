---
description: Project runs in Docker. All Rails/Ruby/DB commands must run inside the web container.
alwaysApply: true
---

# Project Environment

This is a **Dockerized Rails + Inertia + React** project. Nothing runs on the host except the IDE.

## Running Commands

Main service: `web` (Rails app, port 4000).

```bash
# ✅ Correct — run inside container
docker compose exec web bundle exec rails test
docker compose exec web bundle exec rake ts:routes
docker compose exec web bundle exec rubocop -a

# ✅ Frontend tools run on host (Node is local)
yarn tsc
yarn lint

# ❌ NEVER — Ruby/Rails/Bundler are not installed on host
bundle exec rails test
rails console
rake db:migrate
```

## Key Services

| Service | Purpose |
|---------|---------|
| `web` | Rails app (build: `./Dockerfile`) |
| `worker` | Temporal background worker |
| `db` | PostgreSQL |
| `redis` | Redis |
| `traefik` | Reverse proxy / routing |
| `temporal` | Workflow orchestration |

## Local Development

First-time setup installs dependencies into Docker volumes:

```bash
make start   # first time: env files, build, install deps, start all services
make up      # daily: start all services (web, worker, db, redis, temporal, ...)
make doctor  # diagnostics
make down    # stop containers
make reset   # stop and remove volumes (destructive)
```

`make setup` / `make up` create `.env.development` and Playwright `.env` from examples when missing.
`make check-env` prints placeholder warnings (informational, non-blocking).

Daily development: `make up` only. Dependency check uses a lockfile stamp to skip bundle/yarn when unchanged.

## Makefile

Common tasks are in `Makefile`. Targets like `rails-test`, `rubocop-fix` expect to run inside the container. Use `docker compose exec web make <target>` or run individual commands directly.
