---
scope: Federated identity and SSO for the Aixle Flow Rails 8 + Inertia/React app: multi-identity per user, per-company IdP connections, Entra/generic OIDC, passkeys/MFA/magic links, SAML+SCIM via a self-hosted sidecar, server-side sessions, and organization-level auth-provider policy
purpose: build-substrate
altitude: feature
updated: 2026-09-17T16:47
---

- (event by coach) Run started. Intent: create. Brownfield. Driving input: docs/research/technical-federated-identity-and-sso-provider-research-2026-09-16.md
- (direction by user) Working mode: HYBRID — coach on the load-bearing forks, draft the rest with [ASSUMPTION] tags
- (direction by user) Scope: the whole federated-identity feature (Stage 0-4 of the research roadmap), not just the org policy
- (constraint by user) Enterprise SSO must work for self-hosted OSS users, not only the managed cloud
- (constraint by user) No deadline; roadmap-driven. Optimise for total cost of ownership, not time-to-first-SAML-customer
- (constraint by user) No paid external dependency in the login path. Free / self-hostable only
- (constraint by coach) Product is Apache-2.0 and publicly distributed (AixleHQ/flow): anything bundled must be redistributable, anything run in the managed cloud must not create copyleft-as-a-service obligations
- (decision by user) Org auth-provider policy is enforced at COMPANY ENTRY (step-up re-auth), not at the login screen. Binds: policy enforcement. Prevents: a contractor on a foreign email domain bypassing an org's SSO-only requirement, and a multi-company user being locked out by the strictest org's policy at the login screen
- (constraint by coach) Existing reality ratified from code: users.provider+uid is a single unique pair; email is the cross-IdP join key and email_verified is never checked; providers are boot-time initializer constants so per-company connections have nowhere to live; session is only session[:user_id] in the encrypted cookie with no server-side record; companies.email_domain is unique+required so domain->company is already 1:1; companies.settings jsonb exists; membership states are invited/active/suspended/revoked
- (decision by coach) [ADOPTED] Rails stays the system of record for users, companies, memberships and the login UI; external IdPs supply protocol plumbing only. Settled by the research report and the three constraints
- (event by coach) Activation complete; entering coaching on the load-bearing forks
- (direction by user) Deliverables: the spine PLUS a full solution-design document in docs/design/ in the house format (peer of oauth-unification.md), which code comments will cite by section
- (decision by user) Policy unit = a ROW per auth method in a company_auth_providers table (password and Google become seeded rows too), under a deployment-level allowlist ceiling. Binds: every auth method. Prevents: two policy models (flags for built-ins, rows for SAML/OIDC) diverging at Stage 3, and a self-hosted instance offering a toggle for a provider it has no credentials for
- (event by coach) Forks 1 settled; presenting forks 2-4
- (decision by user) Policy is re-evaluated on EVERY request, alongside the existing membership re-validation, and a mismatch redirects to step-up rather than signing the user out. Binds: policy enforcement. Prevents: a second, divergent freshness model next to the membership check, and a revoked connection staying usable for the life of a session
- (decision by user) Break-glass = prove-before-enforce (an SSO-only policy cannot be enabled until an admin of that company has completed a real sign-in through the connection) plus a platform super_admin override. Binds: policy activation. Prevents: an org locking itself out on a misconfigured connection, without weakening SSO-only into a phishable fallback
- (decision by user) Auto-linking a new provider identity to an existing user is allowed ONLY when the asserting connection belongs to the company that owns the verified email domain, and only when the provider asserts email_verified. Everything else requires explicit linking from an authenticated session. Binds: identity resolution. Prevents: whoever controls any accepted IdP inheriting an account by asserting its email
- (version by coach) Verified from the repo: Ruby 4.0.6, Rails 8.1.3.1, omniauth 2.1.4, jwt 3.2.0, rotp 6.3.0
- (constraint by coach) rotp 6.3.0 is declared in the Gemfile (line 185) but has ZERO usages in app/ or lib/ — the TOTP gem is already vendored and approved, and nothing is built on it. Stage 2 inherits it rather than adding a dependency
- (assumption by coach) [ASSUMPTION] Paradigm: ports-and-adapters for auth methods inside the app's existing layered Rails. One port, one adapter per method kind, under app/services/auth/
- (assumption by coach) [ASSUMPTION] SAML reaches Rails only as OIDC from the Ory Polis sidecar; no XML parser and no ruby-saml in the Rails process
- (assumption by coach) [ASSUMPTION] Polis topology: the SAML ACS endpoint must be publicly reachable for a customer IdP to POST to it, while the Polis admin API stays cluster-internal; Polis gets its own database, never the app's
- (assumption by coach) [ASSUMPTION] The deployment-level provider allowlist is CONFIG (Settings/env), not data, so a self-hosted operator cannot be handed a toggle for a provider their instance has no credentials for
- (assumption by coach) [ASSUMPTION] Invitation acceptance is subject to the company's auth policy — accepting an invite is an entry into the company and uses the same gate
- (assumption by coach) [ASSUMPTION] SCIM writes go through the CompanyMembership state machine, never direct SQL, so deprovisioning produces the same audited transitions as the UI
- (assumption by coach) [ASSUMPTION] Session proofs APPEND: a session records every method it has satisfied, and a company is satisfied if the intersection with its currently-enabled methods is non-empty — this prevents step-up ping-pong for multi-company users
- (event by coach) Distilling the spine
- (event by coach) Reviewer gate: lint_spine.py returned 0 findings. Version-reality lens PASS with two flags — MEDIUM: Ory Polis is the only unpinned entry in the Stack while AD-8/AD-9 bind the whole SAML strategy to it; LOW: omniauth-google-oauth2 1.2.3 is missing from the Stack table though google routes through Methods::Oidc. Confirmed accurate: Ruby 4.0.6, Rails 8.1.3.1, omniauth 2.1.4, rotp at Gemfile:185 with zero usages, Encryptable used by OauthCredential/AgentCredential, Audited::Audit used for impersonation, Oauth::State has every claimed property (signed, 10-min TTL, single-use read-then-delete, user-pinned, server-side PKCE verifier)
- (event by reviewer) Rubric-walker lens: CONDITIONAL. All AD file:line citations reread and confirmed accurate
- (question by reviewer) HIGH: AD-4 (a row per method per company, including password and Google) and AD-3 (identity keyed on auth_provider_id+subject) do not reconcile for the two methods that are NOT per-company today — one global Google client, one shared password_digest. A user with one password would need one identity row per company
- (question by reviewer) HIGH: no cutover sequence stated for live cookie sessions that have no Session/session_authentication row once AD-6 lands, nor for the users.provider/uid backfill-then-drop relative to switching sessions_controller#omniauth off the old path. Expand/contract vs stop-the-world is unpicked, though this team uses expand/contract elsewhere
- (question by reviewer) MEDIUM: AD-3/AD-4/AD-10 'only X may do Y' claims name no enforcement mechanism (no cop, no DB constraint), so they are conventions, not constraints
- (question by reviewer) MEDIUM: the research report's open question on IdP-initiated SSO is dropped from the spine entirely — not decided, not deferred, not open
- (question by reviewer) MEDIUM: AD-11's 'same AD-5 gate' does not fit InvitationsController#signup, which mints a brand-new password credential with no prior session or proof to gate against
- (event by reviewer) Adversary lens: NOT READY. 9 compliant-but-incompatible builder pairs found
- (question by reviewer) CRITICAL H1: AD-3's promotion gate never defines email_verified for the CLAIM-ABSENT case; a textually-compliant adapter can treat absence as verified on a generic OIDC connection, reopening the takeover vector AD-3 exists to close
- (question by reviewer) CRITICAL H9: Google is one deployment-wide OAuth app whose company is discovered from the email domain AFTER auth completes; AD-4 read literally forces company-first login and breaks STAGE-0's own entry page. Same root cause as the rubric walker's HIGH on per-company rows for non-per-company methods
- (question by reviewer) HIGH H5: AD-7's guard covers enabled toggles but never row DELETION, and is not company-locked — disable X and delete Y concurrently race to an empty effective set, including an unrecoverable self-inflicted lockout
- (question by reviewer) HIGH H6: SCIM must write user_identities to provision users before their first SSO login, but AD-10 (SCIM via AASM events) and AD-3 (IdentityResolver is the only writer) point opposite ways with no tiebreaker
- (question by reviewer) HIGH H7: in the real code all three invitation-accept call sites run membership.accept! BEFORE any session or proof exists, so AD-11's gate cannot be evaluated where the code puts it. The spine's source sweep also omitted web/invitations_controller.rb
- (question by reviewer) MEDIUM: step-up session lifecycle (new Session row vs appended proof) ambiguous; 'voids immediately' does not rule out a stale cache or sweep-based tombstone; Entra oid is an example rather than a binding claim mapping; no recovery path or messaging rule for a password-only user losing their sole method
- (event by reviewer) Security lens: CONDITIONAL, four CRITICAL — email_domain is an unverified trust anchor for AD-3 promotion; the session token was not stated to rotate on step-up (fixation); AD-7 checked one admin's proof rather than every member's; neither the sidecar nor Entra required verifying the tenant/connection claim against the row it satisfies (confused deputy on two fronts). Plus HIGH on the invitation token's first-redeemer-wins race
- (constraint by coach) Verified: companies are created only in the admin namespace (admin/companies_controller.rb:12); there is no self-serve company creation, which is what makes companies.email_domain trustworthy today and downgrades the trust-anchor finding from exploitable to constrained
- (decision by coach) Gate resolution: AD-3, AD-4, AD-6, AD-7, AD-10, AD-11, AD-12 tightened in place (IDs kept stable); AD-13 (assertion bound to the row that claims it), AD-14 (expand/contract cutover), AD-15 (single-writer rules enforced mechanically), AD-16 (nobody silently loses their last method), AD-17 (domain trust anchor) added. Provider and policy split into identity_providers + company_auth_policies, which resolves both CRITICALs about per-company rows for methods that are not per-company
- (question by coach) Deferred to open questions rather than invented: IdP-initiated SSO (inverts the entry-gate order), and what happens to live cookie sessions at the STAGE-0 deploy — a product call between one forced global re-auth and a bounded adoption window
- (event by coach) Spine re-linted after fixes: 0 findings. Companion solution design written to docs/design/federated-identity.md
- (event by coach) spine finalized
- (decision by user) Open question 5 resolved: at the STAGE-0 cutover every live cookie session is invalidated once — one forced global re-authentication. No legacy-proof concept is built. Binds: AD-14. Prevents: a half-trusted proof shape existing only during a migration window, and two builders inventing different adoption rules for it
- (decision by user) Open question 2 resolved: a passkey belongs to the USER. Read as credential ownership — registration, listing and deletion are the user's alone and no company admin may touch them — while whether a passkey proof satisfies a given company remains a company_auth_policies row, so an SSO-only company can still decline it without destroying the credential. Coach flagged the alternative reading (company toggles do not apply to passkeys at all) for correction
- (event by coach) Spine update intent: fold both resolutions into AD-14 and AD-4/AD-18, drop open questions 2 and 5
- (decision by user) Open question 2 resolved: super_admin bypasses every company auth-policy surface, AND may authenticate only by password. Binds: AD-5, AD-7, AD-18, and the password provider. Prevents: the platform operator being locked out by a customer's policy, and the highest-privilege account depending on an external IdP or on a company-scoped connection whose owner could influence it
- (decision by coach) AD-6 naming amended in implementation: the model is AuthSession/AuthSessionProof, not Session/SessionAuthentication. 'Session' already means an agent terminal session in this codebase (TerminalSession, SessionLog), so the spine's original name would have collided with the product's own vocabulary
- (decision by coach) AD-3 promotion rule amended during implementation: promotion is allowed when the provider asserts email_verified AND EITHER the provider is deployment-scoped (configured by this installation's operator, so its claims carry the operator's own trust) OR it is company-scoped and the email's domain is that company's. Without the first branch an existing password user could never add Google, because our Google client is deployment-wide
- (decision by coach) Implementation finding: params[:provider] in /auth/:provider/callback is OmniAuth's vocabulary (google_oauth2), not ours (google). Auth::Registry.provider_for_omniauth maps it explicitly; treating the URL segment as a provider kind created an unmapped kind and turned every OAuth error into a generic failure
- (decision by coach) Implementation finding: a redirect-provider sign-in by an already-signed-in user APPENDS its proof to the live session instead of starting a new one. Otherwise proving Google would discard the password proof another company still requires, and two companies with disjoint policies would bounce the user between step-ups forever
- (event by coach) CAP-1 and CAP-2 implemented and tested on this branch: 6 migrations, 5 models, 9 services under Auth::, the company-entry gate, the step-up flow, and 64 new tests. Full backend suite, rubocop and brakeman run in an isolated worktree container
- (event by reviewer) Adversarial review of the implementation diff found 2 CRITICAL, 2 HIGH, 1 MEDIUM; multi-tenancy and identity binding came back clean
- (question by reviewer) CRITICAL: the impersonation marker is only a cookie key. sign_out never cleared it (pre-existing), and rotate_session! deliberately carried it across reset_session, so an unrelated later sign-in on the same browser inherited the operator identity and passed authenticate_admin! Verified by temporarily reverting the fix: the bystander got 200 on /admin
- (question by reviewer) CRITICAL: password sign-in and invitation signup never went through Auth::IdentityResolver, so any password set after the backfill migration produced ZERO user_identities. stranded_members reads identities, so PolicyUpdater refused every edit for those companies. The updater's own tests had been seeding the identity by hand, which hid it
- (decision by coach) Fixes: sign_out deletes the impersonation key; rotate_session! no longer carries it; true_user is read off the AuthSession rather than a cookie key; impersonated? requires a live session. Auth::LocalCredential.link! routes password credentials through IdentityResolver, keeping the single-writer rule. Invitations now test user_identities.any? and gate the minted kind on the company's effective set. PolicyUpdater enforces AD-7 rule 4 with the actor's live proof. POST /step_up is throttled
- (event by coach) 26 new tests added for the review findings, including a regression test verified to fail against the pre-fix code
- (decision by coach) CAP-3 built: Microsoft via omniauth-entra-id (deployment-scoped, multi-tenant app) and generic per-company OIDC hand-rolled on the openid_connect gem with PKCE and a server-side nonce. A company connection is NOT an OmniAuth strategy, because strategies are boot-time constants and a customer's issuer is a row
- (decision by coach) AD-4 tightened in implementation: the deployment allowlist is now declared kinds INTERSECTED with Auth::DeploymentProviders.configured_kinds, so a provider whose credentials are absent is never offered. Google was previously in the default allowlist with no credentials configured — the ceiling was a declaration, not a fact
- (decision by coach) CAP-4 built: passkeys (webauthn gem, discoverable credentials, real ceremony in tests via FakeClient), single-use emailed links, and TOTP as a STEP-UP-ONLY method. TOTP never starts a session — it proves possession of a device, not identity — which keeps it inside the OR-shaped proof model without the conjunctive semantics the spine defers
- (decision by coach) Magic links CONFIRM before they consume: the GET renders a button and the POST burns the token. Mail scanners fetch every URL in a message, so a link that signed people in on GET would routinely be dead before its owner clicked it
- (constraint by coach) Implementation trap: this app underscores incoming param keys (ApplicationController#underscore_params), and WebAuthn's wire format is fixed camelCase (rawId, clientDataJSON). The credential is exempted through the documented preserved_param_paths opt-out; without it the gem fails with an opaque NoMethodError on nil
- (constraint by coach) Operational trap: the worktree is bind-mounted into the test container, so editing code while a suite runs makes unrelated tests fail. Finish edits, then run
- (decision by coach) CAP-5 built: SAML reaches the app only as an OAuth code from the sidecar. The connection row stores the bridge's tenant/product keys, not IdP metadata — the bridge is the thing that parses XML. A test pins the invariant directly: Gemfile.lock must contain neither ruby-saml nor omniauth-saml
- (decision by coach) CAP-5 open question 1 is STILL OPEN: there is no live Polis in this environment, so the integration is built against the documented admin/OAuth contract with an app-owned adapter, a WebMock contract test and a compose profile. It is unverified against a running bridge, and the half-day spike still has to happen before anyone relies on it
- (decision by coach) CAP-6 built with scimitar rather than the sidecar's directory sync: a first-party SCIM endpoint lets Entra and Okta push straight to us, with no dependency on the SAML bridge being deployed. That closes the RFC's 'decide after Stage 3' question
- (decision by coach) SCIM resource is a CompanyMembership, not a User: a person can belong to several companies, and a customer's directory owns their presence in ITS company only. runs the same AASM revoke the UI runs, and SCIM never writes user_identities (AD-10 held)
- (constraint by coach) Implementation trap: Scimitar::Resources::Mixin reads its class-level maps at include time, so the include must come AFTER scim_resource_type and friends are defined, or it raises 'You must define ::scim_resource_type'
- (decision by coach) SCIM resource is a CompanyMembership, not a User: a person can belong to several companies, and a customer directory owns their presence in ITS company only. Setting active to false runs the same AASM revoke the UI runs, and SCIM never writes user_identities (AD-10 held)
- (constraint by coach) Implementation trap: Scimitar::Resources::Mixin reads its class-level maps at include time, so the include must come AFTER scim_resource_type and friends are defined, or it raises "You must define ::scim_resource_type"
- (event by reviewer) Second adversarial review (CAP-3..CAP-6): 1 CRITICAL, 1 HIGH, 1 MEDIUM, 2 LOW. Assertion-to-row binding, Auth::State single use, magic-link non-consuming GET, passkey challenge handling, TOTP step-up-only and SCIM tenancy all came back CONFIRMED clean
- (question by reviewer) CRITICAL: scim_user_name was mutable, so a PATCH could repoint an EXISTING membership at an unrelated global account — handing that stranger whatever role the row held, including admin, and detaching the original holder
- (question by reviewer) HIGH: the Microsoft adapter inferred email_verified from 'not the personal-accounts tenant'. Entra does not prove domain ownership and anyone can create a tenant, so an attacker could set a victim's address on their own tenant and be promoted onto the victim's account
- (decision by coach) Fix for the CRITICAL: userName is now IMMUTABLE over SCIM. Closing the rename path removes the attack class outright rather than guarding it, and a directory owning its members' presence is not the same as owning who they are
- (decision by coach) Fix for the HIGH: the Microsoft adapter reports email_verified false unconditionally, so a Microsoft sign-in may CREATE an account but never adopt one. A new LinkRequiredError gives the person a precise instruction instead of a generic failure, and super_admin still gets its own precise refusal
- (decision by coach) Also fixed: SCIM auto-acceptance is limited to the domain the company demonstrably owns (outside it the membership stays invited, like any invitation); throttles added for magic link, SSO discovery, passkey login and OIDC start, and the /auth/ safelist narrowed to callbacks only; the OIDC redirect guard now matches the stricter one already in OauthController
- (event by coach) OPEN QUESTION 1 ANSWERED by a live spike. Brought the bridge up against a throwaway Postgres and registered THREE tenants through the OSS admin API: all three accepted, each with its own clientID. There is no licence gate on per-company connections
- (event by coach) Spike also confirmed the adapter contract: the authorize endpoint accepts our exact client_id=tenant=X&product=Y shape and 302s to the IdP SSO URL with a real SAMLRequest; an unknown tenant resolves to the bridge error page rather than to anything
- (constraint by coach) Spike found a real bug in what was shipped: the compose profile named ghcr.io/ory/polis:latest, which does NOT resolve (manifest unknown). The published image is still the pre-rename boxyhq/jackson:latest. A self-hoster following the file would have got nothing
- (constraint by coach) Spike finding: the bridge enforces one IdP entityID per tenant GLOBALLY — registering the same entityID for a second tenant fails with 'EntityID already exists for different tenant'. Two companies sharing one corporate IdP (subsidiaries) cannot both connect it
- (constraint by coach) Spike finding: the bridge sends anonymous analytics home unless DO_NOT_TRACK=1 or BOXYHQ_NO_ANALYTICS=1 is set. Both are now set in the compose profile — an Apache-2.0 self-hosted product should not phone anywhere by default
- (question by coach) Spike did NOT complete a full SAML round trip: that needs a real IdP to POST a signed assertion, so the token and userinfo halves of the contract remain covered only by the WebMock contract test
